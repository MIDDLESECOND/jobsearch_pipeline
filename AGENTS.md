# AGENTS.md

All agent guidance for this repository lives in **[CLAUDE.md](CLAUDE.md)** — read that
file. It is agent-agnostic despite the name: module layout, commands, architecture
invariants, and conventions all apply to any coding agent working here.

This file is deliberately a pointer, not a copy: the previous full copy silently
drifted from CLAUDE.md between manual syncs. One source of truth, zero sync surfaces.
